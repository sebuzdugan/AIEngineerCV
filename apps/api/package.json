{
  "name": "@aiengineercv/api",
  "version": "0.1.0",
  "private": true,
  "description": "Free-try backend for AIEngineerCV. Holds the OpenRouter key server-side, gates by email code + honor-system follows. Deploy to Vercel.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@vercel/node": "^3.2.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.4"
  }
}
