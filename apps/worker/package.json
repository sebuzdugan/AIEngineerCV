{
  "name": "@aiengineercv/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Free-try backend for AIEngineerCV on Cloudflare Workers. Holds the OpenRouter key server-side, KV for tries + leads, honor-system follows. Deployed from a GitHub Action.",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240909.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.80.0"
  }
}
